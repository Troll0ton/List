#include "../include/list.h"

//-----------------------------------------------------------------------------

int main ()
{
    List lst1;

    list_ctor (&lst1, 15);

    for(int i = 1000; i < 1200; i++)
    {
        stack_push (&lst1, i);
    }

    for(int i = 0; i < 20; i++)
    {
        stack_pop (&lst1);
    }

    debug_stack (&lst1);

    stack_dtor (&lst1);

    return 0;
}

//-----------------------------------------------------------------------------

